mport 'package:flutter/material.dart';
import 'LoginPage.dart';

// first page after opening the app

class HomePage extends StatelessWidget {
  const HomePage({
    super.key,
  });
  //homepage
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      backgroundColor: Colors.white,
        appBar: AppBar(
          backgroundColor: Colors.green,
          leading: Image.asset('assets/images/logo.png',
            width:  200,
          ),
        ),

        body : Center(
          child: Column(
            mainAxisAlignment: MainAxisAlignment.center,
            children:  [
              Image.asset('assets/images/addic image.png',
              width: 140,
              ),
              const Padding(padding: EdgeInsets.all(20)),
              const Text ("ADDICFREE-TABAC",
                style: TextStyle(
                    fontSize: 25,
                    fontFamily: 'Poppins'
                ),
              ),
              //Padding(padding: EdgeInsets.all(10)),
              const  SizedBox(height: 10 ,),

              
              const Text("Vous libérer, c'est notre métier !",
                style: TextStyle(
                  fontSize: 17,
                  fontFamily: 'Poppins'
                ),
                textAlign: TextAlign.center,
              ),
              const SizedBox(height: 80,),
              const Text(
                  " LIBÉRÉ EN\n"
                "1 JOUR, 1 SEMAINE, 1 MOIS ou 1 AN\n"
              "c'est vous qui décidez !",
              style: TextStyle(
                fontSize: 20,
              fontFamily: 'Poppins'),
                textAlign: TextAlign.center,
              ),

              // espace entre le boutton et le texte
              Padding(padding: EdgeInsets.all(30)),

              //signup button
              ElevatedButton.icon(
                style:const  ButtonStyle(
                  //espace dans le boutton
                    padding: MaterialStatePropertyAll(EdgeInsets.all(20)),
                    backgroundColor: MaterialStatePropertyAll(Colors.green)
                ),

                onPressed:() {
                  // navigation a la deuxieme page
                  Navigator.push(
                      context,
                      PageRouteBuilder(
                          pageBuilder: ( _, __, ___) => RegisterPage(),
                      )
                  );
                },
                label: const Text ("Se connecter",
                  style: TextStyle(
                      fontSize: 20
                  ),
                ),
                icon:  const Icon(Icons.login),
              )

            ],
          ),
        )
    );
  }
}
